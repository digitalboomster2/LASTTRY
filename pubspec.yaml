name: savvy_bee
description: Your money, your mind, working together. An AI-powered finance companion.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.9
  go_router: ^12.1.3

  # Firebase
  # firebase_core: ^2.24.2
  # firebase_auth: ^4.15.3
  # cloud_firestore: ^4.13.6
  # firebase_storage: ^11.5.6
  # firebase_messaging: ^14.7.10
  # cloud_functions: ^4.5.8

  # AI & ML
  http: ^1.1.2
  dio: ^5.4.0
  json_annotation: ^4.8.1

  # UI Components
  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.9
  lottie: ^2.7.0
  shimmer: ^3.0.0

  # Utilities
  intl: ^0.19.0
  uuid: ^4.2.1
  file_selector: ^1.0.0
  syncfusion_flutter_pdf: ^29.1.38
  image_picker: ^1.0.7

  # Charts & Analytics
  fl_chart: ^0.66.0

  # Animations
  flutter_animate: ^4.3.0

  # Localization
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.4
  test: ^1.24.9

flutter:
  uses-material-design: true
  assets:
    - assets/videos/
  

